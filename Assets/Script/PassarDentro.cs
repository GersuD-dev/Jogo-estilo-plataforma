     using UnityEngine;
     using System.Collections;
     
     public class PassarDentro : MonoBehaviour 
     {
       /*  Transform rt;

        public int contador;

        private float posicaoX;
        private float posicaoY;

        void Start()
        {
            posicaoX = -2.617f;
            posicaoY = -7.350f;
            contador = 1;
            rt = GetComponent<Transform> ();
        }

         void Update()
         {

             if (pequeno)
             {
                contador = contador + 1;
                rt.localScale = new Vector3 (0.5f,0.5f,0.5f);
                GetComponent<Movimentacao>().forcaPulo = 200f;
             }else
             {
                 if(contador %2 == 0)
                 {
                    Debug.Log(posicaoX);
                    transform.localPosition = new Vector3(posicaoX, posicaoY, 0);
                    contador ++;
                 }
                rt.localScale = new Vector3 (1f,1f,1f);
                GetComponent<Movimentacao>().forcaPulo = 400f;
             }
         }*/

     }

